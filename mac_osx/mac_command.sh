#!/bin/sh

# 1) show hidden path
# https://www.ionos.com/digitalguide/server/configuration/showing-hidden-files-on-a-mac/
# press `cmd + shift + [.] ` in order to show "hidden" path (e.g. /tmp, /opt...)
